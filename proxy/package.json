{
  "name": "kritiq-coach-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Numbers-only Gemini coaching proxy for Kritiq (Cloudflare Worker)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
